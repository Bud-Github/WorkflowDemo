name: Hello World Workflow
on:
  push:
    branches:
      - TestBranch

jobs:
  hello-world-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello World!"     
